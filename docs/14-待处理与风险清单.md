# 14 · 待处理与风险清单

> 用途：项目跟进与排期依据。
> 来源：M1 项目脚手架骨架交付（2026-08-08）验收后登记的待处理/风险项。
> 规则：条目只允许更新状态与验证结果，不删除；新增项追加编号 R6+。编号顺序即优先级（R1 最高），保持不变。

## 当前状态快照（2026-08-08 核实）

- 后端/编辑器 `npm install`、`npm run build` 均通过；后端 `GET /health` 实测 HTTP 200
- 两端 `package-lock.json` 已生成：`project/backend/package-lock.json`、`project/editor/package-lock.json`
- 仓库尚无任何提交（`git status` 全部 untracked），lock 文件尚未进入版本控制
- 后端 `npm install` 报告 22 个漏洞（3 low / 12 moderate / 7 high），均来自嵌套依赖

---

## R1 · 后端 npm audit 高危漏洞（嵌套依赖）

| 字段 | 内容 |
| --- | --- |
| 描述 | `npm install` 报告 7 high / 12 moderate / 3 low，来源为嵌套依赖（如 glob@10.4.5 等），非直接依赖 |
| 状态 | 已登记，未处理 |
| 处理动作 | ① 暂缓：**不执行 `npm audit fix --force`**（会破坏 package.json 版本锁定）② 专项评估排期后：先 `npm audit --json` 导出明细并分类——仅影响开发工具链的 devDependency 传递链 → 升级相关 dev 依赖或记录豁免；影响运行时依赖 → 评估可升级的中间版本后升级直接依赖并回归 ③ 评估结论回写本条目状态（已处理 / 已接受 / 已豁免） |
| 验证方式 | 专项评估完成后 `npm audit` 复核 high 归零或已豁免（须记录豁免理由）；`npm run build` 回归通过 |
| 后续归属 | 后端依赖维护窗口（骨架期后首个专项排期）；触发条件：进入依赖维护排期 |
| 约束 | 禁止 `npm audit fix --force` |

## R2 · package-lock.json 提交与 CI 缓存命中

| 字段 | 内容 |
| --- | --- |
| 描述 | CI 使用 `npm ci` + `cache-dependency-path` 指向 package-lock.json；lock 未提交时 `npm ci` 直接失败、缓存不命中 |
| 状态 | lock 已生成，尚未提交（仓库无任何 commit） |
| 处理动作 | ① 首次 git commit 时确认两个 package-lock.json 均纳入版本控制（覆盖 `project/` 目录即含）② 提交后由 actions/setup-node（cache: npm + cache-dependency-path）命中缓存 |
| 验证方式 | 仓库内 `git ls-files | grep package-lock` 返回两处；CI 日志出现缓存命中、`npm ci` 无报错 |
| 后续归属 | 首次提交发起人（需用户确认后执行）；阻塞 R4 的 CI 首次运行 |
| 依赖 | 用户确认发起首次 git commit |

## R3 · frontend 工程手工创建（Cocos IDE）

| 字段 | 内容 |
| --- | --- |
| 描述 | `project/frontend/` 仅有约定 README，工程依赖 Cocos Creator 3.8 手工创建，无法自动化验证 |
| 状态 | 待手工创建 |
| 处理动作 | ① 按 `project/frontend/README.md`：Cocos Dashboard 新建 3.8.x 空项目 → 路径选 `project/frontend/` → 按 README 的 assets 规划补目录与场景 → 预览端口设 1024 ② 创建后对照 README 自查：`assets/scripts` 七层目录、`bundles` 四类（main/levels/skins/audio） |
| 验证方式 | Cocos 编辑器内打开工程、运行 Boot 场景无报错；目录结构与 README 对照一致（人工自查，**不做自动化验证**） |
| 后续归属 | 客户端开发（手工执行）；M2 关卡解释器开发的前置条件 |
| 约束 | 不做自动化验证；端口 1024 若修改须按 README 全局查重 |

## R4 · CI 首次运行实测（重点：数据库校验 job）

| 字段 | 内容 |
| --- | --- |
| 描述 | CI 三 job（backend 构建 / editor 构建 / database schema 验证）均未在 GitHub Actions 实测；database job 依赖 `mysql:8.0` service 与 16 表断言 |
| 状态 | 未运行（阻塞于 R2 首次提交） |
| 处理动作 | ① 前置：完成 R2 首次提交（含 package-lock.json）② 推送 main/develop 或开 PR，观察 Actions 三 job 结果 ③ 重点验证 database job：脚本幂等执行、`test "$COUNT" = "16"` 断言 ④ 若断言失败：核对表数量与脚本实际 diff，修正断言或脚本后重跑 |
| 验证方式 | GitHub Actions 运行页三 job 全绿；database job 日志显示 `tables=16` |
| 后续归属 | CI 维护者；完成后回写本条目状态 |
| 依赖 | R2 |

## R5 · 编辑器试玩 iframe（Cocos H5 构建产物）

| 字段 | 内容 |
| --- | --- |
| 描述 | 编辑器内试玩 iframe 依赖 Cocos Creator H5 构建产物（web-mobile），属于 M2/M3 能力 |
| 状态 | 骨架期不实现（deferred） |
| 处理动作 | ① 骨架期（M1）不触碰：不实现试玩 iframe、不预留相关代码 ② M2/M3 排期时按 `docs/11-关卡编辑器与内容管线.md` 规划实现（依赖 Cocos H5 构建链路与产物托管） |
| 验证方式 | 不适用（已明确不做）；排期触发后以 docs/11 为准 |
| 后续归属 | M2/M3 编辑器迭代；前置依赖：Cocos 客户端产出 H5 构建链路 |
| 约束 | 骨架期不实现 |

---

## 跟进记录

| 日期 | 操作人 | 动作 | 影响条目 |
| --- | --- | --- | --- |
| 2026-08-08 | Crow5 | 登记 5 项（R1–R5），核实状态快照 | R1–R5 |
